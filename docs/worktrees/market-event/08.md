你现在在 `ibkr_show` 项目中工作。当前任务是实现“市场事件中台”的第八块：**E2E 联调、验收、开源文档与收口**。

注意：本任务不做 Agent 联动，不新增大功能，重点是把前七块做完的能力完整联调、补齐文档、修复明显问题，确保开源用户可以配置并使用“重点事件”功能。

本次 goal 模式会一口气执行 01-08 阶段。BLS / BEA / FRED 的真实 key 不会写在 prompt 中。

如果需要真实 key 做第八块本地验收，请从以下本地文件读取：

~/.ibkr_show_secrets/market_event_keys.env

该文件不在仓库内，禁止复制进仓库。

读取规则：
- 只读取 BLS_API_KEY、BEA_API_KEY、FRED_API_KEY 三个变量。
- 不要 echo、cat、print 真实值。
- 不要把真实值写入日志、文档、测试、最终报告。
- 如果必须确认读取成功，只输出：
  - BLS_API_KEY: configured
  - BEA_API_KEY: configured
  - FRED_API_KEY: configured

写入规则：
- 优先通过已实现的后台数据源配置 API 写入。
- 如果本地后端未启动，可使用项目内部 service/repository 写入加密后的 credential。
- 不允许明文写入数据库。
- 写入后调用 source test 接口验证。
- 最终报告只能展示 masked_value，不展示真实 key。
- 如果读取或写入失败，记录失败原因，但不得打印 key。

## 一、前置背景

前七块已经或应该完成：

```text id="94yp0g"
01_database_model_and_event_schema
02_admin_datasource_config_and_key_encryption
03_provider_abstraction_and_empty_implementations
04_sync_job_framework_and_sync_log
05_connect_real_providers_bls_bea_fed_longbridge
06_market_event_query_api
07_frontend_market_event_pages
```

本次目标是做完整收口：

```text id="l3k8rq"
1. 后端模型 / Provider / 同步 / API 联调
2. 后台数据源配置联调
3. 前台重点事件页面联调
4. 总览页 / 持仓页嵌入联调
5. 同步任务 dry_run 和真实只读同步验证
6. 开源用户配置文档
7. 安全检查
8. 构建、测试、验收清单
```

## 二、强约束

1. 不要实现 Agent 联动。
2. 不要修改交易决策 Agent、每日复盘 Agent、持仓分析 Agent。
3. 不要调用任何交易、下单、账户修改接口。
4. 不要提交 git commit。
5. 不要把任何真实 API Key、token、账户号写入代码、测试、日志、文档。
6. 不要把 `.env`、后台配置里的真实 key、数据库密钥提交到 git。
7. 不要重置或删除数据库。
8. 不要引入大型新依赖，除非前七块已经必须引入且已说明。
9. 如果发现前面步骤遗留问题，优先最小修复，不要重构整个系统。
10. 所有真实同步都必须只读。

## 三、开始前检查

执行：

```bash id="p54e02"
pwd
git status --short
git diff --stat
find . -maxdepth 4 -type f | sed 's#^\./##' | sort | head -300
```

重点确认：

```text id="icsp2l"
1. 市场事件数据库模型是否存在
2. migration 是否存在
3. 后台数据源配置 API 是否存在
4. 后台数据源配置页面是否存在
5. Provider Registry 是否存在
6. Sync Service 是否存在
7. BLS / BEA / Fed / Longbridge Provider 是否存在
8. 事件查询 API 是否存在
9. 前台重点事件页面是否存在
10. 总览页卡片是否存在
11. 持仓详情页事件嵌入是否存在或已说明未实现原因
```

## 四、功能验收清单

逐项检查并修复明显问题。

### 1. 数据库模型验收

确认存在：

```text id="5rlrlc"
market_event_sources
market_event_credentials
market_event_definitions
market_event_occurrences
market_event_values
market_event_impacts
market_event_news_links
market_event_analysis
market_event_sync_runs
```

确认：

```text id="xxj8yb"
1. migration 可执行。
2. 默认 source seed 幂等。
3. dedupe_key 唯一约束有效。
4. raw_payload_hash 稳定。
5. UTC 时间字段使用 timezone-aware datetime。
```

### 2. 后台数据源配置验收

确认后台可以配置：

```text id="brv2bg"
BLS_API_KEY
BEA_API_KEY
FRED_API_KEY
```

确认页面内置申请链接：

```text id="8hds45"
BLS:
https://data.bls.gov/registrationEngine/

BEA:
https://apps.bea.gov/API/signup/

FRED:
https://fred.stlouisfed.org/docs/api/api_key.html
```

确认文档链接：

```text id="gxwe2x"
BLS:
https://www.bls.gov/developers/api_faqs.htm

BEA:
https://apps.bea.gov/api/_pdf/bea_web_service_api_user_guide.pdf

FRED:
https://fred.stlouisfed.org/docs/api/fred/

FED:
https://www.federalreserve.gov/monetarypolicy/fomccalendars.htm

ISM:
https://www.ismworld.org/supply-management-news-and-reports/reports/ism-report-on-business/
```

确认：

```text id="51t5cr"
1. 保存 key 后不回显明文。
2. API response 不包含 encrypted_value。
3. 日志不打印明文 key。
4. 删除 key 后 credential_configured=false。
5. test connection 可安全返回 SUCCESS / FAILED / SKIPPED。
```

### 3. Provider 验收

确认 Provider Registry 包含：

```text id="61rtwe"
BlsProvider
BeaProvider
FredProvider
FedProvider
IsmProvider
LongbridgeProvider
ManualProvider
```

确认能力：

```text id="8jz0dn"
BLS: calendar + value
BEA: calendar + value
FRED: value
FED: calendar
ISM: calendar + value 或预留
LONGBRIDGE: corporate + market holidays + news + trading sessions
MANUAL: manual calendar
```

确认：

```text id="xvka1i"
1. disabled source 会 SKIPPED。
2. 缺少 key 的 BLS/BEA/FRED 会 SKIPPED。
3. 网络失败不会导致服务崩溃。
4. Provider 不返回明文 key。
5. Provider 输出统一 schema。
```

### 4. 同步任务验收

确认：

```text id="ch3yxc"
1. 手动同步可触发。
2. dry_run 不写库。
3. sync_run 记录 RUNNING -> SUCCESS / FAILED / SKIPPED。
4. created_count / updated_count / skipped_count / failed_count 正确更新。
5. 重复同步不会重复插入同一事件。
6. source_code、sync_type、时间范围可以筛选。
7. error_message 脱敏。
```

### 5. 真实只读同步验收

在安全前提下，执行小范围只读同步。

推荐窗口：

```text id="07r4k6"
start_at = 当前时间
end_at = 当前时间 + 30 天
```

测试：

```text id="zsn9sl"
1. BLS calendar dry_run
2. BEA calendar dry_run
3. Fed calendar dry_run
4. Longbridge corporate dry_run
5. Longbridge market holiday dry_run
```

如果用户已经在后台配置了 BLS / BEA key，可以执行小范围真实同步：

```text id="ttn1yo"
1. BLS calendar sync
2. BEA calendar sync
3. Fed calendar sync
4. Longbridge report / dividend / split / ipo sync
5. Longbridge closed / trading_days sync
```

注意：

```text id="8k95dt"
1. 不要执行任何交易、下单、账户查询。
2. 不要打印 key。
3. 如果网络失败，记录失败原因即可。
4. 如果外部接口不可用，不要伪造结果。
```

### 6. 事件查询 API 验收

确认接口可用：

```text id="0rrtvz"
GET /api/market-events
GET /api/market-events/today
GET /api/market-events/upcoming
GET /api/market-events/risk-summary
GET /api/market-events/calendar
GET /api/market-events/{event_id}
GET /api/market-events/symbol/{symbol}
GET /api/market-events/sources
GET /api/admin/market-events/sync-runs
```

确认：

```text id="fce9re"
1. 支持分页。
2. 支持 category / event_type / source_code / importance / symbol / market / keyword 筛选。
3. 默认不返回 raw_payload。
4. 不返回 credential、encrypted_value、API key。
5. risk_summary 风险等级计算正确。
6. calendar 按 display_timezone 分组。
7. symbol 接口能返回直接相关事件和宏观高影响事件。
```

### 7. 前台重点事件页面验收

确认：

```text id="5tkb82"
1. 左侧菜单存在“重点事件”。
2. /market-events 页面可打开。
3. 风险总览卡片可展示。
4. 事件列表可展示。
5. 筛选区可用。
6. 列表/日历或时间线视图可切换。
7. 点击事件可查看详情。
8. 详情展示 values / impacts / news_links / analysis 占位。
9. 数据源状态区可展示。
10. 空状态、错误状态、加载状态正常。
```

### 8. 总览页 / 持仓页嵌入验收

确认：

```text id="3te68a"
总览页:
- 展示未来7天重点事件卡片
- 接口失败不影响总览页主功能
- 无事件时显示空状态

持仓详情页:
- 展示影响该标的的未来事件
- 接口失败不影响持仓详情主功能
```

如果持仓详情页未实现嵌入，必须在最终报告说明原因和后续补点位置。

## 五、安全检查

执行搜索，确认没有泄露 key：

```bash id="0c9jky"
grep -R "BLS_API_KEY=" -n . || true
grep -R "BEA_API_KEY=" -n . || true
grep -R "FRED_API_KEY=" -n . || true
grep -R "LONGBRIDGE_APP_KEY" -n . || true
grep -R "LONGBRIDGE_ACCESS_TOKEN" -n . || true
grep -R "IBKR" -n docs backend frontend 2>/dev/null | head -50 || true
```

注意：

```text id="a6qcpd"
1. 文档中可以出现变量名，例如 BLS_API_KEY。
2. 不能出现真实 key 值。
3. 如果 grep 命中真实值，必须移除。
4. 不要把 grep 输出里的敏感值粘贴到最终报告。
```

检查 API response：

```text id="nl1lai"
1. admin source list 不返回 encrypted_value。
2. public source list 不返回 masked_value。
3. public event detail 不返回 raw_payload，除非 include_raw 且有 admin 权限。
```

## 六、开源文档

新增或更新文档：

```text id="xa01dj"
docs/market_events.md
```

文档内容包括：

```text id="f2uad5"
# 重点事件 / 市场事件中台

## 功能概览
说明该模块用于提前跟踪宏观事件、美联储事件、公司事件、市场休市日和持仓相关新闻。

## 数据源
- BLS：CPI、PPI、非农、失业率、JOLTS
- BEA：GDP、PCE
- FRED：宏观时间序列备用/校验
- Fed：FOMC、SEP、会议纪要
- ISM：PMI
- Longbridge：财报、分红、拆股、IPO、休市、新闻

## API Key 申请
### BLS
申请链接：https://data.bls.gov/registrationEngine/
说明：填写邮箱、组织名、验证码，获取免费 API Key。

### BEA
申请链接：https://apps.bea.gov/API/signup/
说明：填写邮箱和名称，获取 BEA UserID。

### FRED
申请链接：https://fred.stlouisfed.org/docs/api/api_key.html
说明：登录 FRED 账户后申请 API Key。

## 后台配置
进入：
后台管理 > 数据源配置 > 宏观数据源配置

填写：
- BLS_API_KEY
- BEA_API_KEY
- FRED_API_KEY

说明：
- Key 会加密保存在后端
- 前端不会回显明文
- 不要把 key 写入代码或提交到 git

## 同步方式
说明手动同步、dry_run、sync_run 日志。

## 前台使用
说明“重点事件”页面、总览页卡片、持仓详情页嵌入。

## 常见问题
- 为什么 CPI/FOMC 没显示？
- 为什么实际值为空？
- 为什么 Longbridge 有些宏观事件不完整？
- 如何检查数据源是否启用？
- 如何查看同步失败原因？

## 安全说明
- 不要提交 API Key
- 不要公开后台管理入口
- 不要把 raw_payload 暴露给普通用户
```

如果项目 README 有功能列表，也追加一小段“重点事件”说明，但不要写太长。

## 七、开发者文档

新增：

```text id="tw94ig"
docs/market_events_developer.md
```

内容包括：

```text id="dxdfzi"
1. 数据模型关系
2. Provider 架构
3. Sync Service 流程
4. Upsert 幂等规则
5. 风险等级规则
6. 新增数据源 Provider 的步骤
7. 新增事件类型的步骤
8. API 列表
9. 前端页面结构
10. Agent 联动预留点，但明确本次未实现
```

Agent 预留点只写接口说明，不要改代码：

```text id="k2h6on"
未来交易决策 Agent 可以调用：
GET /api/market-events/upcoming
GET /api/market-events/symbol/{symbol}

未来每日复盘 Agent 可以调用：
GET /api/market-events?start_at=...&end_at=...
```

## 八、测试与构建

请根据项目实际执行，不要猜命令。

建议检查：

```bash id="f2p3ql"
git status --short
```

后端：

```bash id="ijfca0"
python -m pytest
```

前端：

```bash id="18m3ji"
npm run build
npm run lint
```

或项目实际使用：

```bash id="odqqtv"
pnpm build
pnpm lint
```

如果有 Docker / Makefile：

```bash id="xyt5fm"
make test
make build
```

请先查看 README、Makefile、package.json、pyproject.toml。

## 九、手动验收脚本或清单

新增：

```text id="7rpz3c"
docs/market_events_acceptance_checklist.md
```

包含手动验收步骤：

```text id="jcx6ii"
1. 启动后端和前端。
2. 登录后台。
3. 打开宏观数据源配置。
4. 检查 BLS / BEA / FRED 申请链接。
5. 填写测试 key。
6. 点击测试连接。
7. 触发 dry_run 同步。
8. 查看 sync_run。
9. 打开重点事件页面。
10. 检查未来事件列表。
11. 打开事件详情。
12. 打开总览页检查未来7天事件卡片。
13. 打开持仓详情页检查相关事件。
14. 检查浏览器控制台无明显报错。
15. 检查后端日志无 key 泄露。
```

## 十、最终报告

完成后输出：

```text id="1cz4pe"
第八块实现结果：

1. 本次修复/新增文件列表
2. E2E 验收结果
3. 数据库 migration 验收结果
4. 后台数据源配置验收结果
5. Provider 验收结果
6. 同步任务验收结果
7. 事件查询 API 验收结果
8. 前台重点事件页面验收结果
9. 总览页 / 持仓页嵌入验收结果
10. 安全检查结果
11. 新增/更新文档列表
12. 已执行测试/构建命令和结果
13. 未完成项或风险
14. 明确说明：Agent 联动本次未实现
15. 后续建议：Agent 联动单独设计后再做
```

## 十一、禁止事项

1. 不要提交 commit。
2. 不要实现 Agent 联动。
3. 不要调用交易、下单、账户修改接口。
4. 不要打印、记录、提交真实 API key。
5. 不要删除或重置数据库。
6. 不要把 raw_payload 默认暴露给普通前台用户。
7. 不要伪造外部数据。
8. 不要为了通过验收隐藏错误。
